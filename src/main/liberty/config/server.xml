<server description="Sample Liberty server">

  <featureManager>
      <feature>microprofile-1.3</feature>
      <feature>openidConnectServer-1.0</feature>
  </featureManager>

  <httpEndpoint httpPort="${default.http.port}" httpsPort="${default.https.port}"
      id="defaultHttpEndpoint" host="*" />

  <!-- Automatically expand WAR files and EAR files -->
  <applicationManager autoExpand="true"/>

  <webApplication name="Auth" location="${app.location}"/>

  <basicRegistry id="basic">
        <user name="foo" password="bar" />
        <user name="user" password="password"/>
        <group name="Admins">
          <member name="foo" />
       </group>
       <group name="Users">
          <member name="user" />
       </group>
  </basicRegistry>

  <!--keyStore id="defaultKeyStore" password="keypass" /-->
  
  <ssl id="defaultSSLConfig" keyStoreRef="bcKeyStore"/>
  <keyStore id="bcKeyStore" location="/etc/keystorevol/BCKeyStoreFile.jks" type="JKS" password="password" />
  
  <openidConnectProvider id="OP" oauthProviderRef="Oauth" signatureAlgorithm="RS256" keyStoreRef="bcKeyStore" keyAliasName="bckey" issuerIdentifier="${env.jwksIssuer}"/>
  <oauthProvider httpsRequired="true" id="Oauth" tokenFormat="mpjwt"  >
    <localStore>
    <client name="bluecomputeweb" secret="bluecomputewebs3cret"
                displayname="bluecomputeweb"
                grant_types="password"
                scope="openid admin blue"
                preAuthorizedScope="openid admin blue"
                enabled="true"
                mapIdentityToRegistryUser="true">
    </client>
    </localStore>
  </oauthProvider>
   <oauth-roles>
    <authenticated>
        <special-subject type="ALL_AUTHENTICATED_USERS" />
    </authenticated>
  </oauth-roles>

</server>
